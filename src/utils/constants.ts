import { colors } from '../styles/colors';
import { radius } from '../styles/constants-size';

export const BUTTON_PADDING = '12px 8px';
export const ACTION_BUTTON_PADDING = '12px 16px';
export const BUTTON_BORDER = '0.5px solid';
export const BUTTON_BORDER_RADIUS = `${radius.radiusLarge}`;
export const ACTION_BUTTON_RADIUS = `${radius.xRadiusLarge}`;
export const BUTTON_HEIGHT = '44px';
export const BUTTON_LARGE_RAIDUS_HEIGHT = '40px';

export const INPUT_PADDING = '12px 4px';
export const INPUT_BORDER_RADIUS = `${radius.radius}`;
export const INPUT_BORDER = `1px solid ${colors.Gray}`;
export const INPUT_BORDER_ERROR = `1px solid ${colors.Red}`;

export const MARGIN_SIDE = '400px';

export const USER_LIST_BORDER_RADIUS = `${radius.radius}`;
export const USER_LIST_BORDER = `0.5px solid ${colors.Gray}`;
