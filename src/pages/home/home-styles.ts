import { styled } from 'styled-components';
import { MARGIN_SIDE } from '../../utils/constants';

const MARGIN_VERTICAL = '60px';
export const Container = styled.div`
  margin: ${MARGIN_VERTICAL} ${MARGIN_SIDE};
`;
